/* Given three prime numbers (p1, p2, p3) and an integer k. Find the first k integers
 * which only have p1, p2, and p3 or a combination of them as prime factors
 *
 * ps = [2, 3, 5] k = 5
 * output: [2, 3, 4, 5, 6]
 */

/*
